<UserControl x:Class="ITU_projekt.Templates.Graph"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:local="clr-namespace:ITU_projekt.Templates"
             xmlns:oxy="http://oxyplot.org/wpf"
             xmlns:viewModels="clr-namespace:ITU_projekt.ViewModels" 
             Height="350" Width="600">

    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="*" />
            <!-- Take remaining space for PlotView -->
            <RowDefinition Height="Auto" />
            <!-- StackPanel at the bottom -->
        </Grid.RowDefinitions>

        <!-- Plot View -->
        <oxy:PlotView Model="{Binding PlotModel}" Grid.Row="0" />

        <!-- X-Axis Range Controls -->
        <StackPanel Orientation="Horizontal" VerticalAlignment="Bottom" Margin="10" Grid.Row="1">
            <!-- Toggle to switch between all or latest N values -->
            <Button Content="Show All" Command="{Binding ToggleRangeCommand}" Margin="5" />

            <TextBlock Text="Latest X values:" VerticalAlignment="Center" Margin="5" />
            <Button Content="-" Command="{Binding DecrementRangeCommand}" Margin="5" />
            <TextBox Width="50" Text="{Binding XAxisRange}" Margin="5" />
            <Button Content="+" Command="{Binding IncrementRangeCommand}" Margin="5" />
        </StackPanel>
    </Grid>
</UserControl>
